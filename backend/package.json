{
  "name": "gosu-chill-backend",
  "version": "0.1.0",
  "private": true,
  "description": "高手揪派 — Cloudflare Workers 後端 API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
