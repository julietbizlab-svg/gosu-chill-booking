# Cloudflare Workers 設定
# 第二階段部署時再填寫

name = "gosu-chill-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 機密變數請放在 .dev.vars（本機）或 Cloudflare Dashboard（正式環境）
# 範例見 .dev.vars.example

[vars]
STUDIO_NAME = "高手揪派"
